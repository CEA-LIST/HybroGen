# IOT Worker

Implement example of continuum computing

Scenario where the code specialization is done on a server (processor
A) which send the binary code to the IOT device (processor B) through
TCP.

    
    